export interface Recipe {
    publisher: string;
    ingredients: string[];
    source_url: string;
    recipe_id: string;
    image_url: string;
    social_rank: number;
    publisher_url: string;
    title: string;
}

